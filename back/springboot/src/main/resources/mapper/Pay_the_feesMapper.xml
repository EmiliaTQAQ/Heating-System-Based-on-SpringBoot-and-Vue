<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dao.Pay_the_feesDao">
    <select id="findBySearch" resultType="com.example.entity.Pay_the_fees">
        select * from Pay_the_fees
        <where>
            <if test="params != null and params.name != null and params.name != ''">
                and name like concat('%',#{params.name},'%')
            </if>
            <if test="params != null and params.account != null and params.account != ''">
                and account like concat('%',#{params.account},'%')
            </if>
        </where>
    </select>

</mapper>